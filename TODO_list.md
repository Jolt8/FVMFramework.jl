TODO:
- Create a means of automatically applying fixed properties to cached properties
- Create a means of automatically applying p to cached properties
- Change framework to handle second order meshes
- Add support for handling moving meshes

Notes:
- Don't implement second order schemes, this simulator was not built for it. They could probably be manually added though if someone really wants it